# Function: isTypeEnum()

> **isTypeEnum**(`type`, `enums`): `boolean`

Defined in: [src/utils/calldata/cairo.ts:77](https://github.com/starknet-io/starknet.js/blob/v10.8.0/src/utils/calldata/cairo.ts#L77)

Checks if a given type is an enum.

## Parameters

### type

`string`

The type to check.

### enums

[`AbiEnums`](../../../../type-aliases/AbiEnums.md)

The enumeration to search in.

## Returns

`boolean`

- True if the type exists in the enumeration, otherwise false.
