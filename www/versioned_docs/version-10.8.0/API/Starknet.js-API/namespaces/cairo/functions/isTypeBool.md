# Function: isTypeBool()

> **isTypeBool**(`type`): `type is "core::bool"`

Defined in: [src/utils/calldata/cairo.ts:127](https://github.com/starknet-io/starknet.js/blob/v10.8.0/src/utils/calldata/cairo.ts#L127)

Checks if the given type is a boolean type.

## Parameters

### type

`string`

The type to be checked.

## Returns

`type is "core::bool"`

- Returns true if the type is a boolean type, otherwise false.
