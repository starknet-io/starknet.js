# Function: isTypeArray()

> **isTypeArray**(`type`): `boolean`

Defined in: [src/utils/calldata/cairo.ts:40](https://github.com/starknet-io/starknet.js/blob/v10.8.0/src/utils/calldata/cairo.ts#L40)

Checks if the given type is an array type.

## Parameters

### type

`string`

The type to check.

## Returns

`boolean`

- `true` if the type is an array type, `false` otherwise.
