# Function: isTypeTuple()

> **isTypeTuple**(`type`): `boolean`

Defined in: [src/utils/calldata/cairo.ts:51](https://github.com/starknet-io/starknet.js/blob/v10.8.0/src/utils/calldata/cairo.ts#L51)

Checks if the given type is a tuple type.

## Parameters

### type

`string`

The type to be checked.

## Returns

`boolean`

- `true` if the type is a tuple type, otherwise `false`.
