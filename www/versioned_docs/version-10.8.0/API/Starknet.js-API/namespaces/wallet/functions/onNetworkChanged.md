# Function: onNetworkChanged()

> **onNetworkChanged**(`swo`, `callback`): `void`

Defined in: [src/wallet/connect.ts:170](https://github.com/starknet-io/starknet.js/blob/v10.8.0/src/wallet/connect.ts#L170)

Register a callback function to be called when the network is changed.

## Parameters

### swo

[`StarknetWindowObject`](../../RPC/interfaces/StarknetWindowObject.md)

The StarknetWindowObject instance.

### callback

[`NetworkChangeEventHandler`](../../RPC/type-aliases/NetworkChangeEventHandler.md)

The callback function to be called when the network is changed.

## Returns

`void`
