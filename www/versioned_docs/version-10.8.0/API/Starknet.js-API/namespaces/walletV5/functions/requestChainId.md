# Function: requestChainId()

> **requestChainId**(`walletWSF`): `Promise`\<`string`\>

Defined in: [src/wallet/connectV5.ts:120](https://github.com/starknet-io/starknet.js/blob/v10.8.0/src/wallet/connectV5.ts#L120)

Request the current chain ID from the wallet.

## Parameters

### walletWSF

`WalletWithStarknetFeatures`

The get-starknet V5 wallet object to use.

## Returns

`Promise`\<`string`\>

The current Starknet chain ID.
