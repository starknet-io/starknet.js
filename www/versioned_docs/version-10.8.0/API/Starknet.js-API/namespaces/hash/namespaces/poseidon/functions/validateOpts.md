# Function: validateOpts()

> **validateOpts**(`opts`): `Readonly`\<\{ `rounds`: `number`; `sboxFn`: (`n`) => `bigint`; `roundConstants`: `bigint`[][]; `mds`: `bigint`[][]; `Fp`: `IField`\<`bigint`\>; `t`: `number`; `roundsFull`: `number`; `roundsPartial`: `number`; `sboxPower?`: `number`; `reversePartialPowIdx?`: `boolean`; \}\>

Defined in: node_modules/@noble/curves/esm/abstract/poseidon.d.ts:13

## Parameters

### opts

[`PoseidonOpts`](../type-aliases/PoseidonOpts.md)

## Returns

`Readonly`\<\{ `rounds`: `number`; `sboxFn`: (`n`) => `bigint`; `roundConstants`: `bigint`[][]; `mds`: `bigint`[][]; `Fp`: `IField`\<`bigint`\>; `t`: `number`; `roundsFull`: `number`; `roundsPartial`: `number`; `sboxPower?`: `number`; `reversePartialPowIdx?`: `boolean`; \}\>
