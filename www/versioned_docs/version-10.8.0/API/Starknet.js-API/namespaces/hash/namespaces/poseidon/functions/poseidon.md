# Function: poseidon()

> **poseidon**(`opts`): \{(`values`): `bigint`[]; `roundConstants`: `bigint`[][]; \}

Defined in: node_modules/@noble/curves/esm/abstract/poseidon.d.ts:26

## Parameters

### opts

[`PoseidonOpts`](../type-aliases/PoseidonOpts.md)

## Returns

\{(`values`): `bigint`[]; `roundConstants`: `bigint`[][]; \}

### roundConstants

> **roundConstants**: `bigint`[][]
